version: '3.8'
services:
  landing_staging:
    build:
      context: .
      args:
        - NEXT_PUBLIC_API_URL=https://papaya-api.travelbuddies.co.id/api
    container_name: tb_landing_staging
    restart: always
    ports:
      - "3001:3000"
    env_file:
      - ./staging/.env
